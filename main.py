"""
Automatic Keyboard

It is a tool for automatically writing a desired text
by specifying the necessary parameters and settings.

This program makes no attempt to hide itself,
so don't use it for keyloggers or online game bots. Be responsible.
"""

from views.home_screen import HomeScreen

if __name__ == "__main__":
    app = HomeScreen()
